[providers]
python = {}

[phases.setup]
nixPkgs = ["...", "nodejs_22"]

[phases.install]
cmds = ["...", "cd JANITY-frontend && npm install"]

[phases.build]
cmds = ["...", "cd JANITY-frontend && npm run build"]
